[pytest]
testpaths = tests
asyncio_mode = auto
# Pinned explicitly: pytest-asyncio warns that this default is changing, and the
# suite relies on each test getting its own loop.
asyncio_default_fixture_loop_scope = function
addopts = -q --tb=short
